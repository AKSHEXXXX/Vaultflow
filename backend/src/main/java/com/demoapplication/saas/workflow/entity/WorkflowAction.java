package com.demoapplication.saas.workflow.entity;

/**
 * Represents the action taken on a document during a workflow transition.
 */
public enum WorkflowAction {
    SUBMITTED,
    APPROVED,
    REJECTED
}
